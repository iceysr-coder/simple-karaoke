{
  "buildCommand": "cd client && npm install && node node_modules/vite/bin/vite.js build",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
